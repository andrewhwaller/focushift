 <div class="card" data-toggle="dragula" data-containers='["card-list-1", "card-list-2", "card-list-3"]'>
          <!-- Card header -->
          <div class="card-header actions-toolbar">
            <div class="actions-search" id="actions-search">
              <div class="input-group input-group-merge input-group-flush">
                <div class="input-group-prepend">
                  <span class="input-group-text bg-transparent"><i class="fas fa-search"></i></span>
                </div>
                <input type="text" class="form-control form-control-flush" placeholder="Type and hit enter ...">
                <div class="input-group-append">
                  <a href="#" class="input-group-text bg-transparent" data-action="search-close" data-target="#actions-search"><i class="fas fa-times"></i></a>
                </div>
              </div>
            </div>
            <div class="row justify-content-between align-items-center">
              <div class="col">
                <h6 class="d-inline-block mb-0"><i class="fas fa-tasks"></i> Projects</h6>
              </div>
              <div class="col-md-6 d-flex align-items-center justify-content-between justify-content-md-end">
                <%= link_to '+', new_project_path, class: 'btn btn-sm btn-success btn-icon-only rounded-circle' %>
              </div>
            </div>
          </div>
          <!-- Card body -->
          <div class="card">
            <div class="card-wrapper p-3">
              <span>
                <% if @projects.empty? %>
                  <%= "Time to create a new project! Use the + button to get started." %>
                <% end %>
              </span>
              <div class="checklist">
              <% @projects.each do |project| %>
                <div class="card border shadow-none">
                  <div class="px-3 py-2 row align-items-center">
                    <div class="col-10">
                      <%= form_for(project) do |f| %>
                        <div class="custom-control custom-checkbox">
                          <%= f.check_box :status, :class => "custom-control-input toggle", :id => project.id, :value => true %>
                      <% end %>
                          <label class="custom-control-label h6 text-sm" for="<%= project.id %>"><%= link_to project.name, project_path(project) %></label>
                      </div>
                    </div>
                    <div class="col-auto card-meta d-inline-flex align-items-center ml-sm-auto">
                      <div class="actions d-inline-block">
                        <div class="mr-2">
                          <span>
                            <i class="far fa-check-circle mr-2"></i>
                            <%= project.tasks.count %> tasks
                          </span>
                        </div>
                      </div>
                      <div class="mr-2">
                        <%= link_to edit_project_path(project), :class => "action-item" do %>
                          <i class="fas fa-edit"></i>
                        <% end %>
                      </div>
                      <div class="mr-2">
                        <%= button_to project_path(project), :method => :delete, :class => "action-item" do %>
                          <i class="fas fa-trash-alt"></i>
                        <% end %>
                      </div>
                    </div>
                  </div>
                </div>
                <% end %>
              </div>
            </div>
          </div>
                        <!-- Empty container -->
          <span class="empty-container" data-placeholder="Empty"></span>
        </div>
      </div>
    </div>
  </div>